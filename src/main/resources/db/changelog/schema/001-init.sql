CREATE TABLE usr (
                         usr_id varchar NOT NULL,
                         role varchar,
                         position varchar,
                         CONSTRAINT pk_user PRIMARY KEY (usr_id)
);